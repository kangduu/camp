import{_ as s}from"./plugin-vue_export-helper-c27b6911.js";import{o as n,c as a,e as o}from"./app-415740f4.js";const t={};function c(d,e){return n(),a("div",null,[...e[0]||(e[0]=[o(`<p>🤔❓ <a href="#%E7%A1%AE%E5%AE%9ANode%E6%96%87%E4%BB%B6%E6%A8%A1%E5%9D%97%E7%B1%BB%E5%9E%8B">如何确认一个 Node.js 文件使用的是<code>ESM</code>还是<code>CommonJS</code>模块</a></p><h2 id="使用-node-执行-js-cjs-mjs-文件" tabindex="-1"><a class="header-anchor" href="#使用-node-执行-js-cjs-mjs-文件" aria-hidden="true">#</a> 使用 node 执行 <code>.js</code> <code>.cjs</code> <code>.mjs</code> 文件</h2><ol><li>默认情况 <code>.js</code> 文件</li></ol><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// hello.js</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">&quot;Hello Node.js&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">node</span> hello.js
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>查找 最近的 <code>package.json</code> 中的 type 值，只要 type 值不为 &#39;module&#39;,那么就是<code>CommonJS模</code></p><ol start="2"><li><code>.cjs</code> <code>.mjs</code></li></ol><p>直接遵循对应模块规则执行。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">node</span> hello.cjs <span class="token operator">|</span> <span class="token function">node</span> hello.mjs
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="如何执行-ts-cts-mts-文件" tabindex="-1"><a class="header-anchor" href="#如何执行-ts-cts-mts-文件" aria-hidden="true">#</a> 如何执行 <code>.ts</code> <code>.cts</code> <code>.mts</code> 文件</h2><p>我们知道一个 Node.js 文件可以是<code>ESM</code>模块，也可以是<code>CommonJS</code>模块。那么在使用<code>tsc</code>、<code>tsx</code>或<code>ts-node</code>进行编译或执行时，也存在各种差异：</p><h3 id="_1-使用-tsc-编译后执行对应的-js-文件" tabindex="-1"><a class="header-anchor" href="#_1-使用-tsc-编译后执行对应的-js-文件" aria-hidden="true">#</a> 1. 使用 <code>tsc</code> 编译后执行对应的 js 文件</h3><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token comment">// index.ts</span>
<span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">&quot;Running TypeScript in Node.js.&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>tsc index.ts <span class="token operator">&amp;&amp;</span> <span class="token function">node</span> index.js
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果我们加上 <code>--noEmit</code> flags，那么将只检测 ts 语法。</p><h3 id="_2-使用-tsx-直接运行-ts-文件" tabindex="-1"><a class="header-anchor" href="#_2-使用-tsx-直接运行-ts-文件" aria-hidden="true">#</a> 2. 使用 <code>tsx</code> 直接运行 ts 文件</h3><div class="language-cts line-numbers-mode" data-ext="cts"><pre class="language-cts"><code>let num = 1;
console.log(num); // 1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-cts line-numbers-mode" data-ext="cts"><pre class="language-cts"><code>let num = 1;
num = &quot;&quot;; // error TS2322: Type &#39;string&#39; is not assignable to type &#39;number&#39;.
console.log(num); // 没有输出日志，process直接退出了
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>_ 不进行类型检查，但执行到类型错误行会退出 _</strong></p><h3 id="_3-使用-ts-node-直接运行-ts-文件" tabindex="-1"><a class="header-anchor" href="#_3-使用-ts-node-直接运行-ts-文件" aria-hidden="true">#</a> 3. 使用 <code>ts-node</code> 直接运行 ts 文件</h3><p>仅<code>.cts</code>可成功，且<code>package.json</code>是<code>CommonJS</code>模块</p><div class="language-cts line-numbers-mode" data-ext="cts"><pre class="language-cts"><code>let a = &quot;hello&quot;;
console.log(a);
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>执行 <code>.ts</code> 之前必须配置 <code>tsconfig.json</code> 文件，否则执行会报错</p><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">let</span> greeting <span class="token operator">=</span> <span class="token string">&quot;hello&quot;</span><span class="token punctuation">;</span>
<span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>greeting<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// export default greeting;</span>
module<span class="token punctuation">.</span>exports <span class="token operator">=</span> <span class="token punctuation">{</span> greeting <span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>执行 <code>.mts</code> 失败，<code>TypeError [ERR_UNKNOWN_FILE_EXTENSION]: Unknown file extension &quot;.mts&quot;</code></p><hr><h2 id="确定-node-文件模块类型" tabindex="-1"><a class="header-anchor" href="#确定-node-文件模块类型" aria-hidden="true">#</a> 确定 Node 文件模块类型</h2><p>👍 在 Node.js 中，确定一个文件是 <strong>ESM（ECMAScript Module）</strong> 还是 <strong>CJS（CommonJS）</strong>，主要有以下几种方式：</p><hr><h3 id="_1-看-package-json-的-type-字段" tabindex="-1"><a class="header-anchor" href="#_1-看-package-json-的-type-字段" aria-hidden="true">#</a> 1. <strong>看 <code>package.json</code> 的 <code>type</code> 字段</strong></h3><ul><li><p>如果最近的 <code>package.json</code> 中有：</p><div class="language-json line-numbers-mode" data-ext="json"><pre class="language-json"><code><span class="token punctuation">{</span>
  <span class="token property">&quot;type&quot;</span><span class="token operator">:</span> <span class="token string">&quot;module&quot;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>那么 <strong><code>.js</code> 文件会被当作 ESM</strong>，除非扩展名是 <code>.cjs</code>（强制 CommonJS）。</p></li><li><p>如果没有定义 <code>type</code>，或者值是 <code>&quot;commonjs&quot;</code>，那么 <strong><code>.js</code> 文件会被当作 CJS</strong>，除非扩展名是 <code>.mjs</code>（强制 ESM）。</p></li></ul><p>✅ 规则：</p><ul><li><code>.js</code> → 跟随 <code>type</code> 配置</li><li><code>.cjs</code> → 永远是 CJS</li><li><code>.mjs</code> → 永远是 ESM</li></ul><hr><h3 id="_2-看文件扩展名" tabindex="-1"><a class="header-anchor" href="#_2-看文件扩展名" aria-hidden="true">#</a> 2. <strong>看文件扩展名</strong></h3><ul><li><code>.cjs</code> → CommonJS</li><li><code>.mjs</code> → ES Module</li><li><code>.js</code> → 依赖 <code>package.json</code> 的 <code>type</code> 字段来判断</li></ul><hr><h3 id="_3-看文件内容" tabindex="-1"><a class="header-anchor" href="#_3-看文件内容" aria-hidden="true">#</a> 3. <strong>看文件内容</strong></h3><p>Node.js 会报错提示你用错了语法：</p><ul><li><strong>CJS 文件</strong>里不能直接用 <code>import</code> / <code>export</code>，否则会报错。</li><li><strong>ESM 文件</strong>里不能直接用 <code>require</code>、<code>module.exports</code>，否则会报错。</li></ul><hr><h3 id="_4-运行时确认-⭐" tabindex="-1"><a class="header-anchor" href="#_4-运行时确认-⭐" aria-hidden="true">#</a> 4. <strong>运行时确认 ⭐</strong></h3><p>你可以在文件里加一句：</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token keyword">import</span><span class="token punctuation">.</span>meta<span class="token punctuation">.</span>url<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 仅 ESM 可用</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token keyword">typeof</span> require<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 在 CJS 中是 &quot;function&quot;，在 ESM 中会报错</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><hr><h3 id="📌-总结一张速查表" tabindex="-1"><a class="header-anchor" href="#📌-总结一张速查表" aria-hidden="true">#</a> 📌 总结一张速查表：</h3><table><thead><tr><th>情况</th><th><code>.js</code></th><th><code>.cjs</code></th><th><code>.mjs</code></th></tr></thead><tbody><tr><td><code>package.json</code> <code>&quot;type&quot;: &quot;commonjs&quot;</code> 或无 <code>type</code></td><td>CJS</td><td>CJS</td><td>ESM</td></tr><tr><td><code>package.json</code> <code>&quot;type&quot;: &quot;module&quot;</code></td><td>ESM</td><td>CJS</td><td>ESM</td></tr></tbody></table><hr>`,49)])])}const p=s(t,[["render",c],["__file","execute.html.vue"]]);export{p as default};
